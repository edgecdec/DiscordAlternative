# Ralph Loop Progress Log
# Append-only. Each iteration records what was done and lessons learned.

## TASK-001: Initialize Next.js project with TypeScript (2026-03-24)
- Created Next.js 15 project with React 19, TypeScript, App Router, src/ directory
- Configured tsconfig.json with @/ path alias → ./src/*
- Created directory structure: src/app, src/components, src/lib, src/hooks, src/types, data/, uploads/
- Updated .gitignore with node_modules, .next, .env, data/, uploads/, .ralph/.server-env
- Minimal layout.tsx, page.tsx, globals.css — stripped all boilerplate
- Added "test" script placeholder to package.json
- Lesson: create-next-app rejects capital letters in directory names — used temp dir + rsync workaround
- Lesson: Another process may reset prd.json status — always re-read before updating

## TASK-002: Install all project dependencies (2026-03-24)
- Installed production deps: @mui/material, @emotion/react, @emotion/styled, @mui/icons-material, socket.io, socket.io-client, prisma, @prisma/client, jsonwebtoken, bcryptjs, cookie, uuid, livekit-server-sdk, @livekit/components-react
- Installed dev deps: @types/jsonwebtoken, @types/bcryptjs, @types/cookie, @types/uuid
- Created minimal prisma/schema.prisma (SQLite datasource, no models yet) so prisma validate passes
- Created prisma.config.ts for Prisma 7 datasource URL configuration
- Lesson: Prisma 7 removed `url` from datasource block in schema.prisma — must use prisma.config.ts with `datasource: { url }` instead
- Lesson: Prisma 7 defineConfig uses `datasource` property (not `migrate`) for the connection URL

## TASK-003: Create Prisma schema with all models (2026-03-24)
- Created prisma/schema.prisma with 5 models: User, Server, Channel, Message, ServerMember
- All relations, cascade deletes (Server→Channel, Server→ServerMember, Channel→Message), indexes (Message.channelId), and @@unique([userId, serverId]) on ServerMember
- Used String fields for role/type (SQLite has no enum support)
- Lesson: Prisma requires reverse relation fields — had to add `ownedServers Server[]` to User for the Server.owner relation
- Note: datasource URL is in prisma.config.ts (Prisma 7 pattern), schema only has `provider = "sqlite"`

## TASK-004: Run initial Prisma migration and create DB client (2026-03-24)
- Ran `npx prisma migrate dev --name init` — created initial migration for all 5 models
- Created src/lib/db.ts with singleton PrismaClient using @prisma/adapter-better-sqlite3
- Installed @prisma/adapter-better-sqlite3, better-sqlite3, @types/better-sqlite3
- Used global caching pattern for dev hot-reload (globalForPrisma)
- Lesson: Prisma 7 requires `adapter` or `accelerateUrl` in PrismaClient constructor — `datasourceUrl` is no longer valid
- Lesson: The adapter class is `PrismaBetterSqlite3` (lowercase "qlite"), not `PrismaBetterSQLite3`
- Lesson: Adapter constructor takes `{ url: "file:/path/to/db" }` object, not a plain string path

## TASK-005: Implement auth helper and JWT utilities (2026-03-24)
- Created `src/lib/auth.ts` with: signToken, verifyToken, getAuthUser (reads cookie via next/headers), buildTokenCookie, buildClearTokenCookie.
- Used Next.js `cookies()` API (async) in getAuthUser instead of raw request header parsing — cleaner for App Router route handlers.
- Exported cookie builder helpers so route handlers can set/clear cookies consistently.
- All 3 validation commands pass (prisma validate, tsc --noEmit, npm test).

## TASK-006: Implement registration API route (2026-03-24)
- Created src/app/api/auth/register/route.ts — POST handler
- Validates username (3-20 alphanumeric), email format, password (min 6 chars)
- Hashes password with bcryptjs (10 rounds), creates User via Prisma
- Signs JWT, sets httpOnly cookie via Set-Cookie header, returns user object with 201
- Checks for duplicate username/email before creating, returns 409 if taken
- All 3 validation commands pass (prisma validate, tsc --noEmit, npm test)
