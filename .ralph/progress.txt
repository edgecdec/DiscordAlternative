# Ralph Loop Progress Log
# Append-only. Each iteration records what was done and lessons learned.

## TASK-001: Initialize Next.js project with TypeScript (2026-03-24)
- Created Next.js 15 project with React 19, TypeScript, App Router, src/ directory
- Configured tsconfig.json with @/ path alias → ./src/*
- Created directory structure: src/app, src/components, src/lib, src/hooks, src/types, data/, uploads/
- Updated .gitignore with node_modules, .next, .env, data/, uploads/, .ralph/.server-env
- Minimal layout.tsx, page.tsx, globals.css — stripped all boilerplate
- Added "test" script placeholder to package.json
- Lesson: create-next-app rejects capital letters in directory names — used temp dir + rsync workaround
- Lesson: Another process may reset prd.json status — always re-read before updating

## TASK-002: Install all project dependencies (2026-03-24)
- Installed production deps: @mui/material, @emotion/react, @emotion/styled, @mui/icons-material, socket.io, socket.io-client, prisma, @prisma/client, jsonwebtoken, bcryptjs, cookie, uuid, livekit-server-sdk, @livekit/components-react
- Installed dev deps: @types/jsonwebtoken, @types/bcryptjs, @types/cookie, @types/uuid
- Created minimal prisma/schema.prisma (SQLite datasource, no models yet) so prisma validate passes
- Created prisma.config.ts for Prisma 7 datasource URL configuration
- Lesson: Prisma 7 removed `url` from datasource block in schema.prisma — must use prisma.config.ts with `datasource: { url }` instead
- Lesson: Prisma 7 defineConfig uses `datasource` property (not `migrate`) for the connection URL

## TASK-003: Create Prisma schema with all models (2026-03-24)
- Created prisma/schema.prisma with 5 models: User, Server, Channel, Message, ServerMember
- All relations, cascade deletes (Server→Channel, Server→ServerMember, Channel→Message), indexes (Message.channelId), and @@unique([userId, serverId]) on ServerMember
- Used String fields for role/type (SQLite has no enum support)
- Lesson: Prisma requires reverse relation fields — had to add `ownedServers Server[]` to User for the Server.owner relation
- Note: datasource URL is in prisma.config.ts (Prisma 7 pattern), schema only has `provider = "sqlite"`

## TASK-004: Run initial Prisma migration and create DB client (2026-03-24)
- Ran `npx prisma migrate dev --name init` — created initial migration for all 5 models
- Created src/lib/db.ts with singleton PrismaClient using @prisma/adapter-better-sqlite3
- Installed @prisma/adapter-better-sqlite3, better-sqlite3, @types/better-sqlite3
- Used global caching pattern for dev hot-reload (globalForPrisma)
- Lesson: Prisma 7 requires `adapter` or `accelerateUrl` in PrismaClient constructor — `datasourceUrl` is no longer valid
- Lesson: The adapter class is `PrismaBetterSqlite3` (lowercase "qlite"), not `PrismaBetterSQLite3`
- Lesson: Adapter constructor takes `{ url: "file:/path/to/db" }` object, not a plain string path

## TASK-005: Implement auth helper and JWT utilities (2026-03-24)
- Created `src/lib/auth.ts` with: signToken, verifyToken, getAuthUser (reads cookie via next/headers), buildTokenCookie, buildClearTokenCookie.
- Used Next.js `cookies()` API (async) in getAuthUser instead of raw request header parsing — cleaner for App Router route handlers.
- Exported cookie builder helpers so route handlers can set/clear cookies consistently.
- All 3 validation commands pass (prisma validate, tsc --noEmit, npm test).

## TASK-006: Implement registration API route (2026-03-24)
- Created src/app/api/auth/register/route.ts — POST handler
- Validates username (3-20 alphanumeric), email format, password (min 6 chars)
- Hashes password with bcryptjs (10 rounds), creates User via Prisma
- Signs JWT, sets httpOnly cookie via Set-Cookie header, returns user object with 201
- Checks for duplicate username/email before creating, returns 409 if taken
- All 3 validation commands pass (prisma validate, tsc --noEmit, npm test)

## TASK-007: Implement login API route (2026-03-24)
- Created `src/app/api/auth/login/route.ts` — POST handler accepting `{ email, password }`.
- Looks up user by email via Prisma `findUnique`, compares password with `bcrypt.compare`.
- On success: signs JWT via `signToken`, sets httpOnly cookie via `buildTokenCookie`, returns user object.
- On failure: returns 401 with generic "Invalid email or password" (no info leakage about which field is wrong).
- Followed exact same patterns as register route (imports, response shape, cookie handling).
- All validation passed: prisma validate ✓, tsc --noEmit ✓, npm test ✓.

## TASK-008: Implement logout and current user API routes (2026-03-24)
- Created `src/app/api/auth/logout/route.ts` — POST handler clears the token cookie using `buildClearTokenCookie()`.
- Created `src/app/api/auth/me/route.ts` — GET handler calls `getAuthUser()`, looks up user in DB via Prisma with select (id, username, email, avatarUrl), returns user or 401.
- Both routes follow the same patterns as login/register (NextResponse, auth helpers from lib/auth).
- All validation passed: prisma validate, tsc --noEmit, npm test.
- Lessons: The auth.ts already had `buildClearTokenCookie()` ready to use — always check existing utilities before writing new ones.

## TASK-009: Create ThemeRegistry and useThemeMode hook (2026-03-24)
- Created `src/hooks/useThemeMode.tsx` — ThemeModeProvider context with dark/light toggle, localStorage persistence.
- Created `src/components/common/ThemeRegistry.tsx` — wraps ThemeModeProvider > ThemeProvider > CssBaseline.
- Primary color #5865F2, dark bg #121212/paper #1e1e1e, light bg #f5f5f5/paper #ffffff.
- Used named constants for all color values (no magic strings).
- Split ThemeRegistry into ThemeModeProvider (context) + ThemeApplier (consumes context, applies ThemeProvider) to avoid circular dependency.
- All validation passed: prisma validate, tsc --noEmit, npm test.
- Lessons: MUI 7 createTheme API is unchanged from v6 — no migration issues.

## TASK-010: Create useAuth hook and AuthProvider (2026-03-24)
- Status: DONE
- Created src/hooks/useAuth.tsx with AuthProvider context and useAuth hook.
- Exposes { user, login, register, logout, loading }.
- On mount calls /api/auth/me to restore session from httpOnly cookie.
- Used useCallback for stable function references, cleanup flag in useEffect to avoid state updates after unmount.
- Lessons: API routes return { user: { id, username, email } } shape — matched the hook's User interface to that. authFetch helper avoids duplicating POST+JSON logic across login/register.

## TASK-011: Create root layout with providers (2026-03-24)
- Updated existing src/app/layout.tsx to wrap children in ThemeRegistry > AuthProvider.
- globals.css already had minimal base styles from TASK-001, no changes needed.
- Lesson: Always check if files already exist before creating — layout.tsx and globals.css were already scaffolded.

## TASK-012: Build login and registration UI (2026-03-24)
- Created src/components/auth/AuthForm.tsx — MUI Card with login/register toggle, username + password fields, validation errors via Alert.
- Updated src/app/page.tsx — redirects to /servers if authenticated, otherwise renders AuthForm centered on screen.
- Uses useAuth hook for submit (login/register) and session check.
- Lessons: Always re-read files before editing — useAuth had already been updated to username-only auth (no email) by a previous commit. The auth spec is the source of truth for field names, not the prd.json task description which mentioned email.

## TASK-055: Create server.js with Next.js wrapper and webhook handler
- Date: 2026-03-25
- Status: done
- Created server.js at project root with custom HTTP server wrapping Next.js request handler.
- Webhook handler at /api/webhook verifies HMAC SHA-256 signature from x-hub-signature-256 header using WEBHOOK_SECRET env var.
- On valid webhook, responds 200 immediately then spawns deploy_webhook.sh asynchronously via execFile.
- Listens on PORT env var (default 3003).
- server.js is plain CommonJS (not TypeScript) since it runs directly with `node server.js`.
- Lessons: server.js is the foundation — Socket.io will be added on top in TASK-027. Keep it minimal for now.

## TASK-056: Create deploy_webhook.sh (2026-03-24)
- Created deploy_webhook.sh with lock file, git fetch/reset, conditional npm install, prisma migrate deploy, next build, pm2 restart.
- Logs to /var/log/webhook_deploy_discord.log.
- Uses md5sum to detect package.json changes — avoids unnecessary npm install on every deploy.
- Trap ensures lock file cleanup even on script failure.

## TASK-057: Create .env.example, set up nginx, and do first deploy
- Date: 2026-03-25
- Status: done
- What was done:
  - .env.example and .gitignore fix were already committed from a previous attempt
  - Created nginx config for discord.edgecdec.com proxying to localhost:3003
  - Obtained SSL cert via certbot (auto-configured nginx for HTTPS)
  - Cloned repo to /var/www/DiscordAlternative on server
  - Generated JWT_SECRET, added to server .env alongside existing WEBHOOK_SECRET
  - Ran npm install, prisma generate, prisma migrate deploy, npm run build
  - Started with pm2 as discord-alt
  - Verified 200 from both localhost:3003 and https://discord.edgecdec.com
  - Fixed deploy_webhook.sh: added `npx prisma generate` before build, changed `npm install --production` to `npm install` (devDeps needed for build)
- Lessons learned:
  - `prisma migrate deploy` does NOT run `prisma generate` — must run generate explicitly before build
  - `npm install --production` skips devDependencies (TypeScript, types) which are needed for `next build` — use `npm install` instead
  - Always check if files were already committed before trying to create them (previous attempt may have done partial work)

## TASK-013: Implement create server API route (2026-03-24)
- Created POST /api/servers with auth, validation, nested Prisma create (server + default general channel + OWNER member).
- Used Prisma nested create to atomically create server, channel, and member in one operation.
- Pattern: getAuthUser() for auth, NextResponse.json for responses, constants for magic values.
- No existing responses.ts/validators.ts helpers yet — followed inline pattern from register route.

## TASK-014: Implement list user servers API route (2026-03-24)
- Added GET handler to src/app/api/servers/route.ts
- Returns servers where user is a member, with channels included and member count via _count
- Ordered by createdAt asc for consistent sidebar ordering
- Lesson: Adding a second handler to an existing route file is straightforward — just export another named function (GET alongside POST)

## TASK-015: Implement server detail API route (2026-03-24)
- Created GET /api/servers/[serverId] — validates membership, returns server with channels and members (including user info).
- Next.js 15 App Router uses `params: Promise<{ serverId: string }>` — must await params before accessing.
- Followed existing pattern from /api/servers/route.ts for auth checks and response shape.
- Membership check via `findUnique` on the `@@unique([userId, serverId])` composite key is clean and efficient.

## TASK-058: Create maintenance.html with Snake minigame (2026-03-24)
- Created self-contained maintenance.html with no external dependencies.
- Snake game uses canvas, arrow keys + WASD, matches app theme (#121212 bg, #5865F2 primary, #1e1e1e game bg).
- nginx serves this via error_page directive on 502/503/504 during deploys.
- Pure HTML/CSS/JS task — no TypeScript or build impact, all validations pass trivially.

## TASK-016: Build server sidebar component
- Date: 2025-03-25
- Status: done
- Created `src/components/layout/ServerSidebar.tsx` — narrow 72px vertical sidebar with server avatars, active state indicator (colored left border + square avatar), and create server button.
- Component accepts `onCreateClick` prop for the create server dialog (TASK-017).
- Fetches servers from `/api/servers` on mount.
- Uses `useParams` to detect active server and highlight it.
- All validation passed first try (prisma validate, tsc --noEmit, npm test).
- Deployment verified: 200 OK.
- Lessons: Keep component props minimal — pass callbacks for actions that will be handled by parent or sibling components (like dialogs).

## TASK-059: Fix database tables missing on production server (2026-03-25)
- Root cause: `prisma.config.ts` had `url: 'file:../data/discord.db'` — in Prisma 7, this resolves relative to the config file (project root), so `../data/` pointed to `/var/www/data/` instead of `/var/www/DiscordAlternative/data/`.
- The app's PrismaClient in `src/lib/db.ts` used `path.join(process.cwd(), 'data', 'discord.db')` which correctly resolved to the project's `data/` dir. Mismatch meant migrations went to wrong file.
- Fix was already applied by human in commit 5fedd2c: changed to `file:./data/discord.db`.
- Verified: deploy webhook ran, migrations applied to correct DB, register endpoint returns valid user.
- Confirmed deploy_webhook.sh will work correctly on future deploys.
- Lessons: In Prisma 7, datasource URL in `prisma.config.ts` resolves relative to the config file location, NOT the schema file. Always verify the runtime DB path matches the migration DB path.

## TASK-060: Create seed script with test user and test credentials file (2026-03-25)
- Created `scripts/seed.js` — standalone Node.js script using Prisma + bcryptjs to seed test users.
- Creates `testbot` (password: testpass123) and `ralphbot` (password: ralphpass123) if they don't exist.
- Ran on production server via SSH — both users created and verified via login API.
- Created `~/.config/discord-alt-creds.env` on local machine with TESTBOT_USER/TESTBOT_PASS and ADMIN_USER/ADMIN_PASS.
- Lessons: Seed script uses CommonJS (require) since it runs directly with `node` outside the Next.js build. Same adapter pattern as src/lib/db.ts but in plain JS.

## TASK-061: Set up Nova Act browser testing for UI verification (2026-03-25)
- Added "Nova Act Testing" section to `.ralph/specs/anti-patterns.md` with the full cookie-based auth bypass pattern.
- Verified the pattern works: curl login → parse `#HttpOnly_` cookie jar → inject via `nova.page.context.add_cookies()` → navigate → auth redirect to `/servers` confirmed.
- Key findings:
  - `ActResult` has only `metadata` — no `.response` attribute. Use `act_get()` with a schema for structured data, or use `nova.page` (Playwright) for assertions.
  - Cookie jar lines prefixed with `#HttpOnly_` must be stripped before parsing — standard `#` comment skip logic will miss them.
  - Use `headless=True` and `max_steps=5` to keep tests fast.
  - Run with `/opt/homebrew/bin/python3.13`.
  - Delete temp scripts after verification.
- Deployment verified: 200 OK (this task only changed a spec file, no runtime impact).

## TASK-062: Create constants.ts with size limits and retrofit existing routes
- Date: 2026-03-25
- Created src/lib/constants.ts with all size limits (USERNAME_MIN/MAX, PASSWORD_MIN/MAX, SERVER_NAME_MIN/MAX, CHANNEL_NAME_MIN/MAX, MESSAGE_MAX, FILE_UPLOAD_MAX_BYTES, AVATAR_URL_MAX, MESSAGES_PER_PAGE, BCRYPT_ROUNDS, USERNAME_PATTERN).
- Updated register route: replaced local constants with imports, added PASSWORD_MAX validation.
- Updated servers route: replaced local SERVER_NAME_MIN/MAX with imports. Note: the old SERVER_NAME_MAX was 100, spec says 50 — corrected to 50.
- Updated AuthForm.tsx: imported constants, replaced hardcoded minLength/maxLength values with USERNAME_MIN/MAX and PASSWORD_MIN/PASSWORD_MAX.
- Login route had no hardcoded limits to replace (it only checks presence, not length).
- Lesson: Always check if existing hardcoded values match the spec — the servers route had SERVER_NAME_MAX=100 but spec says 50.

## TASK-017: Build create server dialog (2026-03-25)
- Created CreateServerDialog.tsx with MUI Dialog, TextField (maxLength from constants), error handling, and navigation on success.
- Created minimal /servers page to wire ServerSidebar + CreateServerDialog together (ServerSidebar already had onCreateClick prop).
- All validation passed first try. Deploy returned 200.
- Lesson: ServerSidebar was already designed with the onCreateClick prop, so integration was straightforward. Keep components composable with callback props.

## TASK-018: Implement create channel API route (2026-03-25)
- Created POST /api/servers/[serverId]/channels with auth + OWNER/ADMIN role check
- Validates channel name (1-50 chars) and type (TEXT/VOICE/VIDEO)
- Followed existing pattern from servers/route.ts — no permissions.ts or responses.ts helpers exist yet, kept inline
- Next.js 15 dynamic route params require `params: Promise<{...}>` and `await params`
- All validation passed first try, deploy returned 200

## TASK-019: Implement update and delete channel API routes (2026-03-25)
- Created src/app/api/channels/[channelId]/route.ts with PATCH and DELETE handlers.
- PATCH accepts { name }, validates OWNER/ADMIN role on the channel's server, updates channel name.
- DELETE validates OWNER/ADMIN, prevents deleting the last TEXT channel (counts TEXT channels in server), soft-deletes via Prisma delete (cascade handles messages).
- Extracted shared `getChannelAndValidateAdmin` helper within the file to avoid duplicating channel lookup + auth + role check logic.
- All validation passed first try (prisma validate, tsc --noEmit, npm test). Deploy returned 200.
- Lesson: Discriminated union return type (`{ error, status } | { channel }`) with `"error" in result` check is a clean pattern for shared validation helpers.

## TASK-020: Build channel list panel component (2026-03-25)
- Created ChannelPanel.tsx (~240px sidebar) with server name header, channels grouped by type (TEXT/VOICE), active channel highlighting, and Add Channel button for OWNER/ADMIN.
- Created CreateChannelDialog.tsx with channel type toggle (TEXT/VOICE) and name input.
- ChannelPanel fetches server data from /api/servers/[serverId] and passes userId prop to determine if user can manage channels.
- Lesson: Keep the "Add" button in the channel group header rather than a separate floating button — matches Discord's UX pattern and keeps the component simpler.

## TASK-021: Build user info panel in channel panel (2026-03-25)
- Created `UserInfoPanel` as a separate component in `src/components/layout/UserInfoPanel.tsx` per one-component-per-file rule.
- Shows Avatar with first letter fallback, username, theme toggle (DarkMode/LightMode), and Settings icon button.
- Uses `useAuth` for user data and `useThemeMode` for toggle.
- Placed at bottom of ChannelPanel with `borderTop` divider and `background.default` bgcolor to visually separate from channel list.
- Removed unused `Divider` import from ChannelPanel after cleanup.
- Lesson: Keep new UI additions as separate components to stay under the 150-line file limit.

## TASK-022: Create three-column layout shell and server pages (2026-03-25)
- Created `src/app/servers/[serverId]/layout.tsx` with three-column flexbox: ServerSidebar (72px) | ChannelPanel (240px) | children (flex:1).
- Created `src/app/servers/[serverId]/page.tsx` that fetches server data and redirects to the first TEXT channel.
- Created `src/app/servers/[serverId]/channels/[channelId]/page.tsx` as a placeholder showing channel name with # icon.
- The layout reuses existing ServerSidebar, ChannelPanel, and CreateServerDialog components — no duplication.
- ChannelPanel already fetches server data using the serverId from useParams, so no prop drilling needed.
- Lesson: The channel page fetches server data to get the channel name. Future tasks (TASK-040) will replace the placeholder content with MessageList/MessageInput.

## TASK-023: Implement get messages API route (2026-03-25)
- Created GET /api/channels/[channelId]/messages with cursor-based pagination
- Validates auth and server membership before returning messages
- Returns messages ordered by createdAt desc, with author info (id, username, avatarUrl)
- Uses MESSAGES_PER_PAGE constant (50) for page size
- Cursor-based pagination: pass `cursor` query param with last message ID to get next page
- Returns `nextCursor` (null when no more pages)
- Lessons: Next.js 15 route params are async (Promise<{channelId}>), must await them. Prisma cursor pagination uses skip:1 + cursor:{id} pattern to avoid returning the cursor item itself.

## TASK-024: Implement create message API route (2026-03-25)
- Added POST handler to existing messages route file
- Validates: auth, content required + non-empty, content length <= MESSAGE_MAX, channel exists, user is server member
- Supports optional fileUrl field
- Returns 201 with message including author info
- Reused existing RouteParams interface and channel/membership validation pattern from GET handler
- Lessons: The GET handler already had the channel lookup + membership check pattern — kept POST consistent with same approach rather than extracting a shared helper (only 2 usages, not worth abstracting yet)

## TASK-025: Implement edit and delete message API routes (2026-03-25)
- Created `src/app/api/messages/[messageId]/route.ts` with PATCH and DELETE handlers.
- PATCH: validates auth, checks author ownership, validates content length against MESSAGE_MAX, returns updated message with author include.
- DELETE: validates auth, checks author OR server admin/owner role, soft-deletes by setting `deleted=true`, returns updated message.
- Both handlers return 404 for already-deleted messages.
- Lessons: The existing messages route pattern (channelId-based membership check) was a good reference. For message-level routes, the delete handler needs an extra hop to look up the channel's serverId to check admin roles — keep this pattern for future message-level operations.

## TASK-026: Create Socket.io type definitions (2026-03-25)
- Created src/types/socket.ts with all payload interfaces and typed ServerToClientEvents/ClientToServerEvents
- Payload types: ChannelJoinPayload, ChannelLeavePayload, MessageCreatePayload, MessageUpdatePayload, MessageDeletePayload, TypingPayload, PresenceOnlinePayload, PresenceOfflinePayload, TypingBroadcastPayload, MessageDeletedPayload, SocketMessage, MessageAuthor
- Lessons: Types-only tasks are straightforward — no runtime impact, just ensure tsc passes. The SocketMessage type includes author info since that's what gets broadcast to clients.

## TASK-027: Add Socket.io to server.js with JWT cookie auth
- Date: 2026-03-25
- Status: DONE
- Changes: Updated server.js to attach Socket.io Server to the HTTP server. Added JWT auth middleware that parses the token cookie from handshake headers, verifies it with jsonwebtoken, and attaches userId/username to socket.data. Rejects unauthenticated connections.
- Lessons: server.js is CommonJS so can't import from src/lib/auth.ts directly — duplicated the JWT verify logic using require('jsonwebtoken') and require('cookie'). The parseAuthFromCookie helper is self-contained and mirrors verifyToken from auth.ts. Future socket event handlers (TASK-028+) will build on the io.on("connection") block added here.

## TASK-028: Implement Socket.io channel join/leave handlers (2026-03-25)
- Added Prisma client initialization to server.js using CommonJS require (PrismaClient + PrismaBetterSqlite3 adapter)
- channel:join validates membership by looking up channel's serverId then checking ServerMember exists for the user
- channel:leave simply calls socket.leave() — no validation needed
- Lesson: server.js is CommonJS so must use require() for Prisma, not the ES module db.ts singleton
- Lesson: The @@unique([userId, serverId]) constraint on ServerMember enables efficient findUnique for membership checks

## TASK-029: Implement Socket.io message:create handler (2026-03-25)
- Added message:create socket handler in server.js
- Validates content (non-empty, max 2000 chars), channel existence, and server membership before persisting
- Creates message via Prisma with authorId from socket.data, includes author info in response
- Broadcasts message:new to channel room with full message object (ISO date strings)
- Lessons: server.js uses CommonJS require() — constants from src/lib/constants.ts can't be imported directly, so the 2000 limit is hardcoded in server.js. If MESSAGE_MAX changes, server.js must be updated too. Consider extracting shared constants to a .js file in the future.

## TASK-030: Implement Socket.io message:update and message:delete handlers
- Date: 2026-03-25
- Status: done
- Added message:update handler: validates author owns message, message not deleted, content valid (1-2000 chars), updates DB, broadcasts message:updated to channel room.
- Added message:delete handler: validates author or server admin/owner/moderator, soft-deletes in DB, broadcasts message:deleted with { messageId } to channel room.
- Lessons: For delete authorization, included MODERATOR role alongside OWNER/ADMIN since moderators should be able to delete messages. Used channel→serverId join to check membership role without extra queries.

## TASK-031: Implement Socket.io presence tracking (2026-03-25)
- Added `onlineUsers` Map<userId, Set<socketId>> to server.js for multi-tab/device tracking.
- On connection: socket joins `server:<serverId>` rooms for all user's memberships, broadcasts `presence:online` only on first socket (user just came online).
- On disconnect: removes socketId from set, broadcasts `presence:offline` only when last socket disconnects (user fully offline).
- Uses `socket.to(room)` for online (excludes sender) and `io.to(room)` for offline (sender already disconnected).
- Lesson: presence:offline uses `io.to()` because the disconnecting socket is already gone from the room — `socket.to()` wouldn't work.

## TASK-032: Implement Socket.io typing indicators (2026-03-25)
- Added `typing:start` and `typing:stop` handlers to server.js.
- Both handlers broadcast to the channel room excluding the sender via `socket.to()`.
- Payload includes `{ userId, username, channelId }` matching the `TypingBroadcastPayload` type.
- Types were already defined in `src/types/socket.ts` from TASK-026 — no type changes needed.
- Lesson: Typing indicators are fire-and-forget — no DB persistence, no membership validation needed beyond being in the room (Socket.io room membership already implies channel access from `channel:join`).

## TASK-033: Create useSocket hook (2026-03-25)
- The useSocket.ts file already existed from a previous iteration (committed in f6f065b) but was never wired into the app.
- The actual work was adding SocketProvider to layout.tsx inside AuthProvider so the socket context is available app-wide.
- Lesson: Always check if a file already exists before creating it — search the git log to avoid redundant work.
- The hook connects to Socket.io when user is authenticated, auto-reconnects, and exposes typed joinChannel/leaveChannel helpers.

## TASK-034: Build MessageList component with initial fetch (2026-03-25)
- Created src/components/chat/MessageList.tsx with channelId prop
- Fetches messages from /api/channels/[channelId]/messages on mount, reverses the desc-ordered response for chronological display
- Renders each message with Avatar, username, timestamp, and content
- Handles deleted messages with italic disabled text
- Auto-scrolls to bottom on load via ref
- Shows loading and empty states
- Lessons: The API returns messages in desc order (newest first) for cursor pagination, so reverse() is needed for chronological display in the UI.

## TASK-035: Add real-time message listeners to MessageList (2026-03-25)
- Added socket listeners for message:new, message:updated, message:deleted to MessageList
- Used useCallback with channelId dependency to filter events to the current channel
- message:new appends and auto-scrolls, message:updated replaces in place, message:deleted sets deleted flag
- Lesson: Keep socket event handlers stable with useCallback to avoid re-registering listeners on every render
- Lesson: Filter incoming socket events by channelId since the socket room may broadcast to channels the component isn't viewing
- Deploy took ~40s to return 200 (500 during rebuild is normal)

## TASK-036: Add cursor-based pagination to MessageList (2026-03-25)
- Used `cursorRef` instead of state for cursor to avoid stale closures in scroll handler.
- `requestAnimationFrame` after prepending older messages to restore scroll position (scrollHeight delta).
- Moved auto-scroll-to-bottom for new messages into `handleNew` callback with `requestAnimationFrame` instead of reacting to `messages.length` change — avoids scrolling to bottom when loading older messages.
- Initial scroll-to-bottom now triggers on `loading` state change (when initial fetch completes) rather than on every `messages.length` change.
- CircularProgress spinner shown at top of list while fetching older messages.

## TASK-037: Build MessageInput component (2026-03-25)
- Created src/components/chat/MessageInput.tsx with MUI TextField + Send IconButton
- Enter sends, Shift+Enter inserts newline, input clears on send
- Uses MESSAGE_MAX from constants.ts for maxLength on input
- Emits message:create via socket with channelId and content
- All validations passed first try, deploy verified at 200
- Lessons: Straightforward component — useCallback for send/keydown keeps it clean

## TASK-038: Add typing indicator emission to MessageInput (2026-03-25)
- Added debounced typing:start emission (max once per 2s via timestamp check)
- Added typing:stop emission after 3s idle timeout or on message send
- Cleanup on unmount/channel change via useEffect return
- Reset lastTypingEmit timestamp on send so next keystroke re-emits immediately
- Named constants TYPING_EMIT_INTERVAL_MS and TYPING_STOP_DELAY_MS (no magic numbers)
- All validations passed first try, deploy verified at 200
- Lessons: useRef is the right tool for mutable values that don't trigger re-renders (timers, timestamps, flags)

## TASK-039: Build TypingIndicator component (2026-03-25)
- Created src/components/chat/TypingIndicator.tsx
- Listens for typing:start/typing:stop events filtered by channelId
- Maintains a Map<userId, username> of currently typing users with 4s auto-clear timeout per user
- Shows "X is typing…" for one user, "N users are typing…" for multiple
- Cleans up all timers and state on unmount/channel change
- Lesson: Keep socket event handlers simple — filter by channelId in the handler, not via separate rooms on the client side.

## TASK-040: Assemble text channel page (2026-03-25)
- Replaced placeholder channel page with full text channel assembly: MessageList, TypingIndicator, MessageInput.
- Added joinChannel on mount and leaveChannel on unmount/channel change via useSocket hook.
- Channel type is fetched from server API; non-TEXT channels show a placeholder message for future voice support.
- Deploy took ~40s to return 200 (500s during rebuild are expected during webhook deploy).
- Lesson: Keep the channel info fetch and socket lifecycle in separate useEffects to avoid coupling data loading with room join/leave.

## TASK-041: Implement invite code API routes (2026-03-25)
- Created GET /api/servers/[serverId]/invite — returns inviteCode for members only
- Created POST /api/servers/invite/[code] — joins server via invite code, creates GUEST member, rejects if already member or invalid code
- Both routes follow existing patterns: auth check, membership validation, proper HTTP status codes (201 for join, 409 for already member, 404 for invalid code)
- Deploy took ~40s to return 200 (500 during rebuild, normal)
- Lessons: Keep routes minimal — no need for extra validation when Prisma unique constraints handle duplicates naturally, but explicit check gives better error messages

## TASK-042: Build invite page (2026-03-25)
- Added GET handler to `/api/servers/invite/[code]` to return server info (name, imageUrl, member count) by invite code — needed for the page to display server details before joining.
- Created `/invite/[code]` page with server avatar, name, member count, and join button.
- If user is not logged in, redirects to login page. If already a member (409), redirects to the server directly.
- Lessons: Keep the GET handler unauthenticated so invite links work for anyone (they still need to log in to actually join). The 409 "already a member" case from POST is a valid redirect path, not just an error.

## TASK-043: Implement list members API route (2026-03-25)
- Created GET /api/servers/[serverId]/members returning members with user info and roles.
- Followed existing pattern from server detail route (auth check, membership check, Prisma query with user select).
- Deploy returned 500 for first 30s during rebuild, then 200 at 40s — normal webhook deploy cycle.
- Lessons: Simple CRUD routes following established patterns are quick wins. The existing server detail route already includes members, but a dedicated members endpoint is needed for the MemberList component (TASK-045).

## TASK-044: Implement change role and kick member API routes (2026-03-25)
- Created `src/app/api/servers/[serverId]/members/[memberId]/route.ts`
- PATCH: Changes member role (OWNER only). Validates role is one of ADMIN/MODERATOR/GUEST. Cannot change OWNER's role.
- DELETE: Kicks member (OWNER/ADMIN only). Cannot kick the OWNER.
- Followed existing patterns from channels route for auth/validation style.
- All validation passed first try. Deploy verified after ~30s (initial 500s during rebuild, then 200).

## TASK-045: Build online members sidebar (2026-03-25)
- Created `src/components/layout/MemberList.tsx` — right sidebar (240px) showing server members grouped by role (OWNER, ADMIN, MODERATOR, GUEST) with green/gray Circle dot for online/offline status.
- Added `presence:list` socket event (client→server request, server→client response) to get initial online user IDs when the component mounts. Without this, the component wouldn't know who's already online.
- Updated `src/types/socket.ts` with `PresenceListPayload` and `PresenceListResponse` types, added `presence:list` to both event interfaces.
- Added `presence:list` handler in `server.js` — validates membership, queries server members, filters by `onlineUsers` map, emits back to requesting socket.
- Wired MemberList into `src/app/servers/[serverId]/layout.tsx` as the rightmost column.
- Hidden on xs/sm screens via `display: { xs: "none", md: "block" }` — mobile responsive task (TASK-054) will handle drawer integration.
- Lesson: When adding real-time features that need initial state, always add a "list" or "sync" event — relying only on incremental updates means late-joining clients miss existing state.

## TASK-063: Bug fix — messages require page refresh to appear (2026-03-25)
- Root cause: channel page's `joinChannel(channelId)` useEffect fired before the socket was connected, so the `channel:join` emit was silently lost (optional chaining on null socket).
- Fix: added `connected` from `useSocket()` as a dependency to the join/leave useEffect in the channel page. Now the effect only fires when `connected` is true, and re-fires if the socket reconnects.
- Lesson: When using Socket.io with React, any emit that must happen on mount needs to be gated on the socket's connected state. Refs don't trigger re-renders, so emits via refs can silently fail if the socket isn't ready yet.

## TASK-046: Implement LiveKit token generation API route (2026-03-25)
- Created `src/app/api/livekit/token/route.ts` — POST endpoint that generates LiveKit access tokens.
- Validates auth, channelId presence, channel existence, channel type (VOICE/VIDEO), and server membership.
- Uses `livekit-server-sdk` AccessToken class with `LIVEKIT_API_KEY`/`LIVEKIT_API_SECRET` env vars.
- Room name format: `channel:<channelId>`, identity: `user:<userId>`, grants: roomJoin, canPublish, canSubscribe, TTL 1h.
- All validation passed first try (prisma validate, tsc --noEmit, npm test).
- Deploy verified immediately (200 on first poll).
- Lessons: `AccessToken.toJwt()` is async (returns Promise) — must await it. The SDK reads env vars automatically if constructor args are omitted, but explicit is better.

## TASK-047: Build VoiceChannel component (2026-03-25)
- Created src/components/voice/VoiceChannel.tsx with LiveKit integration
- Component has 3 states: not joined (join button), connecting (spinner), connected (participant list + controls)
- Uses @livekit/components-react hooks: useParticipants, useLocalParticipant, useConnectionState, useIsSpeaking
- ParticipantTile sub-component shows avatar with green border when speaking, mute icon when muted
- RoomContent sub-component handles mic toggle and disconnect
- RoomAudioRenderer handles remote audio playback
- Token fetched from /api/livekit/token on join
- Lesson: LiveKit components-react provides good hooks but no MUI-compatible styled components — need to build custom UI on top of the hooks
- Lesson: Keep LiveKitRoom as the wrapper and use hooks inside child components (hooks need LiveKit context)

## TASK-048: Wire voice channel into channel page (2026-03-25)
- Updated channel page to render VoiceChannel component for VOICE/VIDEO channels instead of placeholder text
- Added VolumeUp icon for voice/video channel headers, Tag icon for text channels
- Socket join/leave only runs for text channels (voice channels don't need text chat rooms)
- Moved isText/isVoice declarations above the useEffect that depends on them to avoid reference-before-declaration
- Nova Act test confirmed: voice channel shows join prompt, no text input; text channel still works normally
- Lesson: MUI icons don't have data-testid by default — use text content or other selectors for testing

## TASK-049: Implement update server API route (2026-03-25)
- Added PATCH handler to /api/servers/[serverId]/route.ts
- Validates OWNER or ADMIN role before allowing updates
- Accepts optional name and imageUrl fields with validation against constants
- Returns 400 if no fields provided, 403 if not authorized
- Reused existing pattern from channels route for ADMIN_ROLES check
- Lessons: Straightforward addition to existing route file. Pattern is well-established from prior tasks.

## TASK-050: Implement delete server API route (2026-03-25)
- Added DELETE handler to src/app/api/servers/[serverId]/route.ts
- Only OWNER role can delete; returns 403 for non-owners
- Prisma cascade deletes handle channels, messages, and members automatically
- Returns 204 No Content on success
- Verified on production: 401 without auth, 403 for non-owner, route works correctly
- Lesson: During deploy transition, endpoints may briefly return unexpected status codes (405) — always retest after build completes

## TASK-051: Build server settings dialog — general tab (2026-03-25)
- Created ServerSettings.tsx with rename field and invite code copy-to-clipboard
- Added Settings gear icon to ChannelPanel header (visible to OWNER/ADMIN only)
- Wired dialog open/close and onUpdated callback to refetch server data
- Lesson: testbot lands on /servers (list page) not directly on a server channel — Nova Act tests need to handle clicking into a server first before testing channel panel features
- Lesson: After git push, the maintenance page may show briefly during rebuild — wait extra time before curling for auth cookies against the live site

## TASK-052: Add members tab to server settings (2026-03-25)
- Created MembersTab.tsx as a separate component to keep ServerSettings under 150 lines
- Added Tabs (General/Members) to ServerSettings dialog
- OWNER sees role change dropdown (ADMIN/MODERATOR/GUEST) and kick button per member; non-owners see role chips
- Passed userRole from ChannelPanel's currentMember to ServerSettings
- Nova Act test confirmed: dialog opens, Members tab visible, clicking it shows member list
- Lesson: Extracting tab content into separate components keeps dialog files manageable and focused

## TASK-053: Add danger zone tab to server settings (2026-03-25)
- Created DangerZoneTab component with red-styled delete button and confirmation dialog requiring server name input
- Added conditional "Danger Zone" tab to ServerSettings (only visible to OWNER role)
- Confirmation dialog requires typing the exact server name before delete is enabled
- On successful deletion, redirects to /servers
- Lessons: Nova Act guardrails block prompts containing words like "Danger" — use Playwright directly for filling inputs with sensitive-sounding text. Keep act() prompts neutral.

## TASK-054: Add mobile responsive layout with Drawer (2026-03-25)
- Used `useMediaQuery("(max-width:767px)")` raw query since MUI's `md` breakpoint is 900px, not 768px.
- Added `onNavigate` optional callback to both ServerSidebar and ChannelPanel so the drawer closes when user navigates.
- Hamburger button positioned absolutely in the content area; channel page header gets extra left padding on mobile to avoid overlap.
- MemberList hidden on mobile entirely (not in drawer) to save space.
- Nova Act doesn't support mobile viewports (<1280px width) — can only verify desktop regression, not mobile behavior directly.
- Drawer uses `PaperProps` with `flexDirection: "row"` to lay out ServerSidebar and ChannelPanel side by side inside the drawer.

## TASK-064: Create user profile API routes (2026-03-26)
- Created GET /api/users/[username] for public profiles, PATCH /api/auth/profile for username/avatar updates, PATCH /api/auth/password for password changes.
- When username changes, re-sign JWT and set new cookie so the token stays in sync.
- All validation uses constants from src/lib/constants.ts.
- Deploy webhook build takes ~30s on the server — need to wait for build to complete before testing API routes (initial curl may return 404 if build is still running).

## TASK-065: Build user profile page (2026-03-26)
- Created `/profile/[username]` page showing avatar, username, join date, and edit button for own profile.
- Created `ProfileDialog` component with two tabs: Profile (username + avatar URL) and Password change.
- Added `refreshUser()` to `useAuth` hook so profile edits update the global user state.
- Wired Settings button in `UserInfoPanel` to open `ProfileDialog`.
- Lesson: Using `TransitionProps.onEnter` on MUI Dialog is a clean way to reset form state when the dialog opens, avoiding stale state from previous opens.
- Lesson: Keep dialog components in `src/components/common/` when they're reused from multiple places (profile page + user info panel).

## TASK-066: Track last-read message per channel per user (2026-03-26)
- Added ChannelRead model to Prisma schema with @@unique([userId, channelId])
- Created PATCH /api/channels/[channelId]/read — upserts lastReadMessageId to latest message
- Updated channel page to mark channel as read on mount and on each new message received via socket
- Lesson: After adding a new Prisma model, must run `npx prisma generate` before `tsc --noEmit` — the migration alone doesn't update the client types
- Lesson: When no messages exist in a channel, the read endpoint correctly returns success without creating a ChannelRead record (no message to reference)

## TASK-067: Add unread indicators to channel list and server sidebar (2026-03-26)
- Created GET /api/servers/[serverId]/unreads endpoint that compares lastReadMessageId timestamps against newer messages per channel
- Updated ChannelPanel: bold text + primary-colored dot for unread channels (suppressed for currently active channel)
- Updated ServerSidebar: MUI Badge dot on server avatars when any channel has unreads (suppressed for active server)
- Both components poll unreads every 30 seconds via setInterval
- Lesson: The deploy webhook deletes .next before rebuilding, causing brief 500s. Always wait 60+ seconds before checking deployment status.
- Lesson: For unread detection, comparing createdAt timestamps after looking up the lastReadMessageId is more reliable than ID-based comparison since cuid IDs aren't sequential.

## TASK-068: Implement file upload API route (2026-03-26)
- Created POST /api/upload with multipart form data handling, UUID filenames, and FILE_UPLOAD_MAX_BYTES validation
- Added static file serving in server.js for /uploads/ path with proper MIME types and cache headers
- Used path.basename() to prevent directory traversal in static serving
- Lesson: server.js needs `fs` require added for static file serving — remember to add imports when extending server.js
- Lesson: Next.js formData() API works well for file uploads, no need for external multipart libraries

## TASK-069: Add file attachment button to MessageInput (2026-03-26)
- Added AttachFile IconButton to MessageInput with hidden file input
- Upload uses XHR for progress tracking (XMLHttpRequest upload.progress event)
- Pending file shown as a chip with close button above the input
- File size validated client-side against FILE_UPLOAD_MAX_BYTES before upload
- Socket message:create already supported fileUrl in types — just needed to pass it
- Lesson: Nova Act test needs to start on about:blank before injecting cookies, then navigate — starting directly on the target URL doesn't pick up cookies set after page load
- Lesson: The /servers page (no server selected) doesn't show MessageInput — need to navigate into a specific channel to verify chat UI components

## TASK-070: Display image attachments inline in messages (2026-03-26)
- Created `MessageAttachment.tsx` component to handle both image and non-image file rendering
- Images (jpg/jpeg/png/gif/webp) render inline with max-width 400px, clickable to open in new tab
- Non-image files render as download links with a file icon
- Extracted into separate component to keep MessageList under 150-line limit (it was already at 185 lines)
- Used MUI theme tokens (action.hover, action.selected, primary.main) instead of hardcoded colors
- Lesson: Nova Act has trouble with file input elements — use Playwright's `set_input_files` directly instead of trying to click file inputs through Nova Act
- Lesson: To test attachment rendering, create a message with fileUrl via API first, then verify the UI renders it

## TASK-071: Create message reactions database model and API (2026-03-26)
- Added Reaction model to Prisma schema with @@unique([userId, messageId, emoji]) and @@index([messageId])
- Added relations to User and Message models
- Created POST /api/messages/[messageId]/reactions — toggles reaction (add if not exists, remove if exists)
- Created GET /api/messages/[messageId]/reactions — returns reactions grouped by emoji with count and userReacted boolean
- Both endpoints validate server membership before allowing access
- Lesson: After adding a new Prisma model, must run `npx prisma generate` to regenerate the client before tsc will pass — the migration alone doesn't update the TypeScript types

## TASK-072: Add reaction UI to messages (2026-03-26)
- ReactionBar component with emoji picker popover (24 common emoji in 8-col grid)
- Reaction chips below messages show emoji + count, highlighted if current user reacted
- Clicking a chip toggles the reaction via socket emit (reaction:toggle)
- Add-reaction button appears on message hover
- MessageList listens for reaction:add/reaction:remove socket events and updates state in real-time
- Server.js reaction:toggle handler broadcasts to channel room
- Lesson: Task was already fully implemented from a previous interrupted iteration — just needed status update. Always check git log before re-implementing.

## TASK-073: Add reply support to messages database and API (2026-03-26)
- Added `replyToId` optional field to Message model with self-relation (onDelete: SetNull)
- Updated GET /api/channels/[channelId]/messages to include replyTo (id, content snippet, author username, deleted flag)
- Updated POST /api/channels/[channelId]/messages to accept optional replyToId
- Updated Socket.io message:create handler in server.js to persist and broadcast replyTo data
- Updated SocketMessage and MessageCreatePayload types in socket.ts
- Lesson: After `prisma migrate dev`, must also run `prisma generate` for TypeScript types to update — the migration alone doesn't regenerate the client types locally
- Lesson: Deploy webhook may temporarily show 500 while .next is being rebuilt — poll with retries instead of checking once

## TASK-074: Build reply UI in chat (2026-03-26)
- Created ReplyQuote component to render reply-to snippets above messages
- Added reply button (hover-visible) to each message in MessageList
- Added reply bar above MessageInput showing "Replying to <username>" with cancel button
- Lifted reply state to channel page, passed as props to MessageList and MessageInput
- MessageInput sends replyToId when replying, clears reply state on send
- Reply state resets on channel change
- Lessons: Nova Act hover actions fail with INVALID_INPUT errors — use Playwright's .hover() directly instead. MUI TextField's textarea selector needs get_by_placeholder, not generic textarea selector (hidden textarea confuses Playwright).

## TASK-075: Implement @mention parsing and highlighting (2026-03-26)
- Added `MentionNotifyPayload` type and `mention:notify` event to socket types
- Server-side: In `message:create` handler, parse @username patterns from message content, look up server members, emit `mention:notify` to each mentioned user's sockets (excluding sender)
- Client-side: Created `MentionText` component that splits message content on `@(\w+)` regex and renders mentions as highlighted `<span>` elements with `primary.main` color and `action.hover` background
- Updated `MessageList` to use `MentionText` for rendering non-deleted message content
- Lessons: Starting Nova Act from `about:blank` then injecting cookies before navigating avoids race conditions. The deploy script takes ~60s to rebuild, so 500s during that window are expected.

## TASK-076: Add @mention autocomplete to MessageInput (2026-03-26)
- Created MentionAutocomplete.tsx as a simple Popper with filtered member list
- Updated MessageInput to accept serverId prop, fetch members, and track @-trigger position via cursor analysis
- Keyboard navigation: ArrowUp/Down to navigate, Tab/Enter to select, Escape to dismiss
- Selecting a member inserts @username with a trailing space and restores cursor position
- Had to pass serverId from channel page to MessageInput — added it as a new required prop
- The getMentionQuery helper uses regex to detect @word pattern before cursor position
- Lesson: MUI TextField with multiline uses a textarea, so selectionStart works correctly for cursor tracking
- Lesson: Using requestAnimationFrame to set cursor position after React re-render ensures correct placement

## TASK-077: Add link preview rendering to messages (2026-03-26)
- Updated MentionText to parse URLs alongside @mentions using a combined regex, rendering URLs as MUI Link components with target="_blank".
- Created LinkPreview component showing domain name with a link icon in a bordered card (max-width 400px).
- Integrated into MessageList: for non-deleted messages, extracts the first URL and renders a LinkPreview below the content.
- Lesson: Combining URL and mention regex into one pass avoids double-parsing and keeps the component simple. The IIFE pattern `{(() => { ... })()}` works for inline conditional rendering with local variables in JSX.

## TASK-078: Add emoji picker to MessageInput (2026-03-26)
- Created EmojiPicker.tsx as a separate component with ~100 emoji in 4 categories (Smileys, Gestures, Objects, Nature) using MUI Popover.
- Integrated into MessageInput with EmojiEmotions icon button between TextField and Send button.
- Emoji inserts at cursor position in the input field using selectionStart.
- Nova Act first attempt failed because the agent's click didn't properly trigger the MUI Popover — using Playwright's direct `.click()` on the aria-label selector worked reliably.
- Lesson: For MUI Popover/Popover components, use Playwright direct clicks in Nova Act tests rather than relying on the agent's visual click, which may miss the exact target.

## TASK-079: Add channel categories to database and API (2026-03-26)
- Added ChannelCategory model with id, name, position, serverId fields
- Added optional categoryId to Channel model (onDelete: SetNull so channels survive category deletion)
- Created POST /api/servers/[serverId]/categories — auto-increments position
- Created PATCH/DELETE /api/categories/[categoryId] — supports name and position updates
- Updated channel creation to accept optional categoryId with validation that category belongs to same server
- Added CATEGORY_NAME_MIN/MAX constants
- Lesson: After schema changes, must run `npx prisma generate` before tsc will pass — migrate dev alone doesn't always update the local client types fast enough
- Lesson: Deploy webhook deletes .next before rebuilding, so 500s are expected for ~60s after push — poll longer before panicking

## TASK-080: Render channel categories in ChannelPanel (2026-03-26)
- Updated GET /api/servers/[serverId] to include `categories` in the Prisma query (ordered by position).
- Rewrote ChannelPanel to group channels by categoryId. Uncategorized channels appear at top under a generic "Channels" header. Each category is collapsible via MUI Collapse with expand/chevron icons.
- Category headers show channel count (e.g. "General — 3").
- Collapsed state stored in local React state (Record<categoryId, boolean>).
- Lesson: The server API didn't include categories — always check what the API returns before building UI that depends on new data shapes.

## TASK-081: Build first-time user onboarding flow (2026-03-26)
- Created JoinServerDialog component at src/components/layout/JoinServerDialog.tsx — parses both raw invite codes and full invite URLs.
- Updated src/app/servers/page.tsx to fetch server count and show a welcome page with "Create a Server" and "Join a Server" buttons when user has no servers.
- When user has servers, the normal sidebar + "Select or create a server" view is shown.
- Lesson: The servers page already fetched servers in ServerSidebar, but the page itself needed its own fetch to determine whether to show onboarding vs normal view. Kept it simple with a single /api/servers call.

## TASK-082: Implement message search API (2026-03-26)
- Created GET /api/servers/[serverId]/search?q=query with cursor-based pagination
- Returns messages with author info (id, username, avatarUrl) and channel info (id, name)
- Filters out deleted messages, validates server membership
- Returns 400 for empty/missing query param
- Reused existing patterns: getAuthUser, MESSAGES_PER_PAGE constant, cursor pagination
- Lesson: Simple Prisma `contains` filter works well for SQLite text search — no need for complex full-text search setup

## TASK-083: Build message search UI (2026-03-26)
- Created SearchDialog component with debounced search, result snippets, pagination ("Load more"), and click-to-navigate.
- Added search IconButton to channel header with aria-label for accessibility.
- Search results show author avatar, username, date, content snippet with query context, and channel name.
- Clicking a result navigates to the channel (scrolling to specific message not implemented — would need MessageList changes for anchor scrolling).
- Lessons: Keep dialog components self-contained with their own state management. Debounced search avoids hammering the API on every keystroke.

## TASK-084: Add keyboard shortcuts (2026-03-26)
- Implemented Cmd+K / Ctrl+K quick channel switcher dialog
- Added global keyboard shortcut hook (useKeyboardShortcuts) that auto-focuses message input on printable key press
- Escape closes dialogs (MUI handles this natively, hook covers quick switcher explicitly)
- Added keyboard shortcut hint icon (bottom-right corner, tooltip with shortcut list)
- Lesson: MUI ListItemButton renders as div with role="button", not li — use role-based selectors in tests
- Lesson: Nova Act tests need extra wait time (2s) for API fetches inside dialogs to complete
- Lesson: testbot user lands on /servers (onboarding) not inside a server — tests need to click into a server first

## TASK-085: Add user status to database and API (2026-03-26)
- Status: DONE
- Added `status` field to User model (default "online"), valid values: online/away/dnd/offline
- Created PATCH /api/auth/status route with validation against USER_STATUSES constant
- Added presence:status Socket.io event handler in server.js — broadcasts to all user's servers
- Updated presence:list to return userStatuses map alongside onlineUserIds
- MemberList already shows colored status dots (green=online, yellow=away, red=dnd, gray=offline)
- Lessons: This task was already fully implemented and committed in a previous iteration but status wasn't updated to "done". Always verify prd.json status is updated before terminating.

## TASK-086: Build user status selector UI (2026-03-26)
- Created StatusSelector component with clickable status dot overlay on user avatar
- Dot shows current status color (green=online, yellow=away, red=dnd, gray=invisible)
- Clicking opens MUI Menu with 4 status options: Online, Away, Do Not Disturb, Invisible
- Selecting a status calls PATCH /api/auth/status and emits presence:status via Socket.io
- Added status field to /api/auth/me response so UserInfoPanel can load initial status
- UserInfoPanel listens for presence:status socket events to stay in sync
- MemberList already showed status dots from TASK-085 — no changes needed there
- Added aria-label="Change status" and tooltip for accessibility
- Lesson: Nova Act struggles with tiny click targets (14px dots) — use Playwright locators with aria-label for precise element interaction instead of visual act() instructions
- Lesson: Always add aria-label to small interactive elements for both accessibility and testability

## TASK-087: Create direct message database model and API (2026-03-27)
- Added DirectMessage model to Prisma with sender/receiver relations and composite indexes
- Created GET /api/dm/[userId] for paginated DM history between two users
- Created POST /api/dm/[userId] for sending DMs with content validation
- Created GET /api/dm/conversations for listing recent DM partners sorted by last message
- Added dm:create Socket.io handler in server.js that broadcasts dm:new to both sender and receiver sockets
- Added DM types (DirectMessagePayload, DMCreatePayload) and events to socket.ts
- Lesson: Prisma client needs regeneration after schema changes before tsc will pass — always run `npx prisma generate` after `npx prisma migrate dev`

## TASK-088: Build direct messages UI (2026-03-27)
- Added ChatBubble icon to ServerSidebar above server list with active state highlighting (matches server icon pattern)
- Created /dm layout with ServerSidebar (reuses existing component)
- Created /dm page showing recent DM conversations list with user avatars and last message timestamps
- Created /dm/[userId] page with DM chat view using dedicated DMMessageList and DMMessageInput components
- DMMessageList listens for dm:new socket events filtered by otherUserId for real-time message delivery
- DMMessageInput emits dm:create via socket (server.js already handles this from TASK-087)
- DMMessageList supports cursor-based pagination (scroll to top to load older messages)
- Used usePathname from next/navigation for reactive DM active state detection in sidebar
- Lesson: MessageList and MessageInput are tightly coupled to channel concepts (channelId, serverId, reactions, replies, mentions, typing indicators) — creating simpler DM-specific versions was cleaner than trying to make the channel components generic
- Lesson: Deploy webhook may rebuild during test window — first Nova Act test hit a rebuild cycle and failed, second attempt passed after waiting for build to complete

## TASK-089: Create audit log database model and API (2026-03-27)
- Added AuditLog model to Prisma schema with serverId, userId, action, targetId, details fields
- Created src/lib/auditLog.ts helper with typed AuditAction union and logAudit() function
- Created GET /api/servers/[serverId]/audit-log — OWNER/ADMIN only, cursor-based pagination
- Added audit logging to: member role change, member kick, channel create, channel delete, admin message delete, server update
- Lesson: After adding a new Prisma model, must run `npx prisma generate` before tsc will recognize the new model on the client — `migrate dev` alone doesn't update the generated client in all cases
- Lesson: Message delete audit only logs admin-initiated deletes (not self-deletes) to keep the log meaningful

## TASK-090: Build audit log UI in server settings (2026-03-26)
- Created AuditLogTab component with paginated list of audit log entries
- Added Audit Log tab to ServerSettings dialog, visible to OWNER and ADMIN roles
- Tab shows user avatar, action description, parsed details, and timestamp
- Supports cursor-based pagination with "Load More" button
- Lessons: MUI Tabs with conditional children auto-assigns sequential indices to rendered tabs — no manual index mapping needed. Owner implies Admin so Danger Zone tab index is always 3 when Audit Log is present. Use `domcontentloaded` instead of `networkidle` for Nova Act page.goto when Socket.io keeps connections alive.

## TASK-147: Install and configure LiveKit server on production (2026-03-26)
- Installed LiveKit v1.10.0 binary at /usr/local/bin/livekit-server on the production server.
- Generated API keys and created /etc/livekit.yaml config.
- LiveKit panics with `invalid argument to Intn` when port_range is only 1 port (7881-7881). Fixed by using range 50000-50100.
- LiveKit runs internally on port 7890. nginx proxies wss://discord.edgecdec.com:7880 → http://127.0.0.1:7890 with SSL termination and WebSocket upgrade headers.
- Opened firewall ports: 7880 (TCP for WSS via nginx), 7881/udp (RTC TCP), 50000-50100/udp (ICE range).
- Added LIVEKIT_API_KEY, LIVEKIT_API_SECRET, NEXT_PUBLIC_LIVEKIT_URL=wss://discord.edgecdec.com:7880 to server .env.
- Rebuilt the app so NEXT_PUBLIC_LIVEKIT_URL is baked into the client bundle.
- Updated .env.example to use wss:// instead of ws://.
- Lesson: LiveKit needs a proper UDP port range for ICE candidates, not a single port. Also, if you want WSS, use nginx SSL termination on a separate port rather than trying to share the port with LiveKit.

## TASK-091: Implement slow mode for channels (2026-03-27)
- Added `slowModeSeconds` Int field (default 0) to Channel model in Prisma
- Updated PATCH /api/channels/[channelId] to accept `slowModeSeconds` (0-300) alongside `name`, made both fields optional
- Added SLOW_MODE_MAX_SECONDS=300 constant
- Added slow mode enforcement in server.js message:create handler: queries user's last message in channel, rejects with `message:error` event if within cooldown window
- OWNER/ADMIN roles are exempt from slow mode
- Added `MessageErrorPayload` and `message:error` event to socket types
- Lesson: When updating a PATCH route to accept new fields, make all fields optional and only update what's provided — avoids breaking existing callers that only send `name`.

## TASK-092: Build slow mode UI (2026-03-26)
- Created ChannelSettingsDialog.tsx with slow mode duration selector (off, 5s, 10s, 30s, 1m, 5m)
- Updated MessageInput to accept slowModeSeconds prop, listen for message:error events from server, show countdown timer in placeholder, disable input during cooldown
- Updated channel page to pass slowModeSeconds to MessageInput, show Schedule icon in header when slow mode active, add Settings gear button for OWNER/ADMIN
- The server already enforced slow mode and emitted message:error — just needed the client-side UI
- Lesson: The server detail API returns all channel fields by default (no select clause), so slowModeSeconds was already available without API changes
- Lesson: Nova Act navigation can be tricky — the first icon in sidebar might be DM, not a server. Need to be specific in instructions.

## TASK-093: Implement message pinning API (2026-03-27)
- Task was already fully implemented in a previous iteration but left as in_progress
- Verified: POST /api/messages/[messageId]/pin toggles pinned status (OWNER/ADMIN/MODERATOR only)
- Verified: GET /api/channels/[channelId]/pins returns all pinned messages
- Verified: Socket.io pin:toggle event handler in server.js broadcasts to channel room
- Verified: PinTogglePayload and PinToggleClientPayload types defined in socket.ts
- Verified: pinned Boolean field exists on Message model in Prisma schema
- All APIs tested on production — pin/unpin/get pins all work correctly
- Lesson: Always check if a task marked in_progress was already completed before re-implementing

## TASK-151: Persist voice channel connection across page navigation (2026-03-27)
- Created VoiceProvider context (useVoice.tsx) to hold global voice state (channelId, token, channelName, serverId)
- Created GlobalVoiceRoom component that wraps children in LiveKitRoom when voice is active, using display:contents so it doesn't affect layout
- Created VoiceBar component showing "Voice Connected" with channel name and disconnect button, placed above UserInfoPanel in ChannelPanel
- Rewrote VoiceChannel to use global VoiceProvider instead of managing its own LiveKitRoom — ConnectedView uses LiveKit hooks from the ancestor LiveKitRoom context
- Key insight: LiveKitRoom must wrap descendant components for hooks to work — used display:contents style to make it a layout-transparent wrapper
- Added VoiceProvider to root layout wrapping GlobalVoiceRoom wrapping children, so LiveKit context is available app-wide
- Lesson: When lifting state from a page component to a global provider, ensure context providers wrap the tree correctly for hooks that depend on them

## TASK-148: Add noise suppression, AGC, echo cancellation, and DTX to voice channels
- Date: 2026-03-27
- Status: done
- Changes: Updated GlobalVoiceRoom.tsx to pass `options` prop to LiveKitRoom with audioCaptureDefaults (noiseSuppression, autoGainControl, echoCancellation) and publishDefaults (dtx: true).
- Lessons: LiveKitRoom accepts an `options` prop of type `RoomOptions` which includes `audioCaptureDefaults` and `publishDefaults`. These are typed as `AudioCaptureOptions` and `TrackPublishDefaults` from `livekit-client`. Using module-level constants with useMemo avoids recreating the options object on every render.

## TASK-149: Preserve invite redirect through registration flow (2026-03-27)
- Fixed: unauthenticated users clicking invite links now get redirected back to the invite after login/register.
- The invite page already passed `?redirect=/invite/CODE` to the home page — the home page just wasn't reading it.
- Used `useSearchParams()` in page.tsx to read the `redirect` query param and pass it to `router.replace()` after auth.
- Required wrapping the component in `<Suspense>` because `useSearchParams()` needs it in Next.js App Router.
- Lesson: When using `useSearchParams()` in Next.js 15 App Router, always wrap in Suspense or the build will warn/fail.

## TASK-150: Fix image/file upload failing (2026-03-27)
- Root cause: nginx default `client_max_body_size` is 1MB, but our FILE_UPLOAD_MAX_BYTES is 5MB. Any file over 1MB was rejected by nginx with 413 before reaching the app.
- Fix: Added `client_max_body_size 6m;` to the nginx server block for discord.edgecdec.com (6MB to give headroom above the 5MB app limit).
- The upload API route, server.js static file serving, and MessageInput attachment flow were all correct — the issue was purely nginx config.
- Lesson: When debugging upload failures, always check the reverse proxy (nginx) body size limits first. The default 1MB is very common to hit.
- Verified: Small files, 2MB files, 5MB files all upload successfully. 5.5MB files correctly rejected by the app with 400 error. Uploaded files accessible via /uploads/ URL.

## TASK-094: Build pinned messages UI (2026-03-27)
- Created PinnedMessagesDrawer component with real-time pin:toggle socket updates
- Added PushPin icon button in channel header to open the drawer
- Added pin/unpin button on message hover (visible only for OWNER/ADMIN/MODERATOR roles)
- Pinned messages show a small pin icon next to the timestamp
- MessageList now listens for pin:toggle socket events to update pinned state in real-time
- Passed userRole from channel page to MessageList to control pin button visibility
- Lesson: Use wait_until="load" instead of "networkidle" for Nova Act tests on pages with WebSocket connections — networkidle never resolves due to persistent socket connections
- Lesson: Navigate directly to a known channel URL in Nova Act tests instead of relying on the agent to find UI elements on complex layouts

## TASK-095: Show edit indicator on edited messages (2026-03-27)
- Added "(edited)" label next to message timestamp when updatedAt !== createdAt and message is not deleted
- Wrapped in MUI Tooltip showing the edit timestamp on hover
- Styled with italic and cursor:default to match Discord's pattern
- SocketMessage type already had updatedAt field, no type changes needed
- Lesson: Minimal UI changes like this are low-risk — just compare two existing fields and conditionally render a span

## TASK-096: Implement custom server emoji upload (2026-03-27)
- Added ServerEmoji model to Prisma schema with @@unique([serverId, name]) constraint
- Created GET/POST /api/servers/[serverId]/emoji and DELETE /api/servers/[serverId]/emoji/[emojiId]
- Added emoji constants: EMOJI_NAME_MIN=2, EMOJI_NAME_MAX=32, EMOJI_NAME_PATTERN, EMOJI_MAX_BYTES=256KB
- Emoji images stored in uploads/emoji/ subdirectory with UUID filenames
- DELETE also cleans up the file from disk
- Lesson: After adding a new Prisma model, must run `npx prisma generate` before tsc will recognize the new model on PrismaClient
- Lesson: After deploy webhook runs, sometimes need to wait or restart pm2 for new API routes to be served (build cache)

## TASK-097: Integrate custom emoji into emoji picker and messages (2026-03-27)
- Added "Server" tab to EmojiPicker that fetches and displays custom emoji for the current server
- Updated MentionText to parse `:emoji_name:` syntax and render custom emoji as inline images
- Custom emoji inserted as `:name:` text in MessageInput, rendered as images in MessageList
- Used a simple in-memory cache (Map) in MentionText to avoid re-fetching emoji on every message render
- Lesson: The EmojiPicker only shows the Server tab when custom emoji exist — no empty tab shown
- Lesson: Need to pass serverId through the component chain (ChannelPage → MessageList → MentionText) for emoji rendering

## TASK-098: Implement thread creation from messages (2026-03-27)
- Added Thread model to Prisma schema with parentMessageId (unique), channelId, creatorId, archived, name fields
- Added threadId field to Message model for thread messages, plus spawnedThread reverse relation
- Created POST /api/messages/[messageId]/thread — creates thread from a message, returns existing thread if already spawned, prevents creating threads from thread messages
- Created GET/POST /api/threads/[threadId]/messages — paginated thread message retrieval and creation with auth/membership checks
- Updated GET /api/channels/[channelId]/messages to filter out thread messages (threadId: null) and include spawnedThread info with message count
- Added MessageThread type to socket.ts and thread field to SocketMessage
- Added THREAD_NAME_MIN/MAX constants
- Lesson: After prisma migrate dev, must run prisma generate before tsc --noEmit will pass — the client types aren't updated until generation runs
- Lesson: curl without Accept header against Next.js API routes may return HTML 404 pages instead of JSON — use cookie jar (-b flag) for proper auth

## TASK-099: Build thread UI (2026-03-27)
- Created ThreadPanel component (src/components/chat/ThreadPanel.tsx) — side panel with message list, input, pagination
- Added "Create Thread" button (ChatBubbleOutline icon) to MessageList message hover actions
- Added thread reply count indicator below messages that have threads (clickable to open thread)
- Wired ThreadPanel into channel page as a flex sibling beside the main chat area
- Thread panel uses REST API (POST /api/messages/:id/thread to create, GET/POST /api/threads/:id/messages for messages)
- Thread panel also listens for socket message:new events filtered by threadId for real-time updates
- Lessons: Thread messages are sent via REST (not socket) since they go to a different endpoint than channel messages. The socket listener still picks up new thread messages broadcast to the channel room.

## TASK-100: Show who's in voice channels (2026-03-27)
- Implemented voice channel participant tracking via Socket.io events (voice:join, voice:leave, voice:participants)
- Server-side: in-memory Maps track which users are in which voice channels, with cleanup on disconnect
- Client-side: VoiceProvider emits voice:join/voice:leave when connecting/disconnecting from LiveKit
- Created VoiceChannelParticipants component that listens for real-time updates and shows avatars/usernames under voice channels in ChannelPanel
- Lesson: When adding socket events, need to update 3 places: types/socket.ts (types), server.js (handlers), and the emitting component
- Lesson: Nova Act starting_page should be "about:blank" when injecting cookies before navigation to avoid auth redirect issues
